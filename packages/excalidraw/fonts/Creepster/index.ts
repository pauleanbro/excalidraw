import { GOOGLE_FONTS_RANGES } from "@excalidraw/common";

import { type ExcalidrawFontFaceDescriptor } from "../Fonts";

export const CreepsterFontFaces: ExcalidrawFontFaceDescriptor[] = [
  {
    uri: "https://cdn.jsdelivr.net/npm/@fontsource/creepster/files/creepster-latin-400-normal.woff2",
    descriptors: { unicodeRange: GOOGLE_FONTS_RANGES.LATIN },
  },
];
